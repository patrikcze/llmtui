// Package eval contains opt-in, bounded evaluation runners for configured
// local model endpoints. It records diagnostic metadata and aggregate
// outcomes without storing prompts, reasoning, credentials, or tool payloads.
package eval
